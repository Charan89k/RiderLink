<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only. Permits cleartext to the Android emulator's host loopback
  alias so a local livekit-server in dev mode and token server can be used without
  certificates. Release builds keep Android's default: HTTPS and WSS only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
